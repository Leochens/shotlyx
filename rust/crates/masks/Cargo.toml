[package]
name = "masks"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"

[lib]
path = "src/masks.rs"
crate-type = ["rlib"]

[dependencies]
bytemuck = { version = "1.25.0", features = ["derive"] }
gpu = { version = "0.1.0", path = "../gpu" }
wgpu = "29.0.1"
